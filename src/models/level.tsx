/**
 * Interface representing a level.
 */
export interface Level {
  /**
   * The name of the level
   */
  name: string,
  /**
   * The array representation of a level.
   */
  map: Array<Array<number>>
}
